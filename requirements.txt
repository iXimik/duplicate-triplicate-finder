# Core (Tkinter is in stdlib)
pillow>=10.0
imagehash>=4.3
opencv-python>=4.8 ; platform_system=="Windows"  # optional for video similarity


Примечание: imagehash подтянет numpy, scipy, PyWavelets автоматически.